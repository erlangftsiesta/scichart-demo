import React from "react";
import {
  Box,
  Typography,
  Paper,
  FormControlLabel,
  Switch,
  Divider,
} from "@mui/material";

const SettingsPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: "bold" }}>
        Settings
      </Typography>
    </Box>
  );
};

export default SettingsPage;
